{
    "name": "zomato-clone-application",
    "version": "1.0.0",
    "scripts": {
        "backend": "uvicorn phases.phase_3_api.backend.api.main:app --host 0.0.0.0 --port 8000 --reload",
        "frontend": "npm run dev --prefix phases/phase_4_frontend",
        "dev": "npx concurrently \"npm run backend\" \"npm run frontend\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}